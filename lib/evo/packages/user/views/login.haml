= render_block 'User', 'Provide your credentials below to login.' do
  - form :login do
    - text :name, :label => 'Username', :required => true
    - password :password, :label => 'Password', :required => true
    - buttons do
      - submit :op, 'Login'